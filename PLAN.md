# TokenShield 生产化计划书

## 目标

构建一个部署在桌面 Agent 与大模型 API 之间的可观测上下文优化代理。系统必须满足：兼容现有 Agent、默认安全可回退、用户数据最小化采集、每项优化可量化、每个版本可独立验证。

## 非目标

- 不训练新的基础模型；
- 不修改桌面 Agent 的业务逻辑；
- 不把“工具输出压缩比例”等同于总成本节省；
- 不在未脱敏、未授权的情况下上传用户原文；
- 不以 demo 指标替代真实任务成功率。

## 交付原则

1. 小步提交：每个提交只解决一个可验收问题。
2. 先可观测、后优化：没有 baseline 就不启用压缩。
3. 先复用、后自研：优先接入成熟开源组件，保留适配层。
4. 可逆：任何优化都能关闭、回退或取回原文。
5. 可验证：每个能力有自动化测试、真实请求验证和指标定义。
6. 隐私优先：默认只上传聚合指标，原文本地保存并设置 TTL。

## 里程碑

### M0 工程基线（当前）

- 项目结构、配置、测试、运行说明；
- 健康检查和基础 API 兼容入口；
- Git 基线提交。

### M1 透明代理和可靠观测

- 非流式和流式 `/v1/chat/completions` 透传；
- request/session/user 关联 ID；
- provider-specific tokenizer 与价格配置；
- SQLite 事件模型、Prometheus 指标；
- 上游超时、重试、错误分类；
- 验收：不启用优化时，行为和上游 API 一致。

### M2 安全的工具输出处理

- shell、测试日志、git diff、JSON 四类结构化处理器；
- 原文哈希、TTL、加密和本地全文检索；
- 压缩结果和原文之间的可逆关联；
- 验收：压缩前后任务成功率、工具调用成功率可对比。

### M3 上下文策略引擎

- 内容分类、大小阈值、会话阶段和历史重复度判断；
- 压缩、保留、索引、丢弃四种动作；
- 失败自动回退原文；
- 逐用户、逐会话 A/B 开关；
- 验收：任何单项策略可独立关闭。

### M4 缓存和桌面 Agent 适配

- 精确缓存和可选语义缓存；
- OpenAI-compatible 之外的适配层；
- MCP 工具 schema 按需加载；
- 验收：缓存命中不改变任务语义，并可审计命中原因。

### M5 生产化和试点

- 鉴权、限流、租户隔离、配置中心；
- Prometheus/Grafana 或公司内部观测平台；
- 脱敏审计、数据保留策略、运维手册；
- 压测、故障演练和灰度发布；
- 验收：小规模真实用户试点，收益和回退率达到预设门槛。

## 核心指标

- `input_token_reduction_rate`：输入 token 减少率；
- `cost_reduction_rate`：按真实价格计算的成本减少率；
- `task_success_rate`：任务成功率；
- `tool_call_success_rate`：工具调用成功率；
- `fallback_rate`：回退到原文的比例；
- `cache_hit_rate`：缓存命中率；
- `p50/p95_latency_ms`：延迟；
- `raw_data_upload_rate`：上传原文比例，目标默认 0。

任何节省率都必须同时报告任务成功率和失败/回退率。

## 技术路线

- 代理：FastAPI/HTTPX，后续支持 streaming；
- 存储：SQLite 起步，生产可切换 PostgreSQL；
- 指标：Prometheus/OpenTelemetry；
- tokenizer：按 provider 插件化；
- 现有能力适配：RTK、LLMLingua、GPTCache、slim-mcp、LiteLLM；
- 自研差异化：可逆压缩、桌面 Agent 内容分类、策略引擎、自动回退、收益观测。

## 每个提交的验收模板

```text
目标：
变更：
风险：
自动化测试：
手工验证：
指标变化：
回滚方式：
```

